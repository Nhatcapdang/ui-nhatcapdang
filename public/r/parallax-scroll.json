{
	"$schema": "https://ui.shadcn.com/schema/registry-item.json",
	"name": "parallax-scroll",
	"type": "registry:component",
	"title": "Parallax Scroll",
	"description": "A parallax scroll effect component that creates a smooth sliding animation with text overlays as the content scrolls into view.",
	"dependencies": [
		"motion"
	],
	"files": [
		{
			"path": "src/components/scroll/parallax-scroll.tsx",
			"content": "'use client'\n\nimport { motion, useScroll, useSpring, useTransform } from 'motion/react'\nimport { useRef } from 'react'\n\n\ninterface ParallaxScrollProps {\n    offset?: ['start center', 'center start'] | ['center', 'center'],\n    children: React.ReactNode\n}\nexport default function ParallaxScroll({ offset = ['center', 'center'], children }: ParallaxScrollProps) {\n    const ref = useRef(null)\n    const { scrollYProgress } = useScroll({\n        target: ref,\n        offset,\n    })\n\n    const y = useTransform(scrollYProgress, [0, 1], ['10%', '0%'])\n    const ySpring = useSpring(y, {\n        stiffness: 100,\n        damping: 10,\n        mass: 1,\n    })\n\n    return (\n        <div ref={ref} className=\"h-[90vh] relative overflow-hidden\">\n            <h1 className=\"md:text-8xl xs:text-6xl text-4xl font-bold text-center\">NHATCAPDANG</h1>\n            <motion.div\n                style={{ y: ySpring }}\n                className=\"h-[82vh] absolute top-0 inset-x-0 z-10\"\n            >\n                {children}\n            </motion.div>\n            <h1 className=\"md:text-8xl xs:text-6xl text-4xl font-bold text-center absolute bottom-0 left-0 right-0\">NHATCAPDANG</h1>\n        </div>\n    )\n}\n",
			"type": "registry:component",
			"target": "src/nhatcapdang/parallax-scroll.tsx"
		}
	]
}
